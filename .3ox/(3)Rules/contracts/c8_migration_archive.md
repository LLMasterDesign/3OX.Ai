---
# status:[DRAFT] auth:[3OX.AI]
# C8 migration archive — stub (law: old_versions_remain_replayable).
---

# contracts/c8_migration_archive

Keep **versioned** contract snapshots with **hash** and optional **sign** step. Every migration emits a **receipt** so replay can walk `contract.history` without silent drift.
