---
# status:[DRAFT] auth:[3OX.AI]
# C1 Vec3 runtime — stub narrative for Ring.C C1 (law: vec3_is_runtime_geometry).
---

# contracts/c1_vec3_runtime

Agent **surface** resolves through `.vec3/rc` geometry: `lib`, `dev`, `var` as addressable state. Path resolution failures ν{path.resolve}. Surface authority λ{surface.authority} is owned by the station profile, not ad-hoc cwd.

## Intent

Bind **κk04** ENCODE.AUTHORITY to concrete rc paths and capability slices referenced from `routes.json` / `(5)Links`.
