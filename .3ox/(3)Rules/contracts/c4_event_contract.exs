# Stub — Ring.C C4 event contract (law: event_driven_not_polling).
# Elixir surface placeholder; no GenServer until runtime package exists.

defmodule Contracts.C4EventContract do
  @moduledoc "Classify external wake sources (fs, queue, rpc, receipt, clock) — spec only."
end
